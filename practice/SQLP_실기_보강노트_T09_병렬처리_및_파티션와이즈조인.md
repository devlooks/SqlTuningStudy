# 📖 [T09] 병렬 처리(Parallel) & 파티션-와이즈 조인(PWJ) 마스터 보강노트

- **테마:** `T09 Partition-Wise Join / Parallel Execution`
- **기준 마스터 문서:** `AGENTS.md`, `SQLP_실기_2회독_통합패턴맵_v2_3_최종확정.md`
- **문서 목적:** 실전 시험 직전 병렬 쿼리 슬레이브 간 통신(`PQ_DISTRIBUTE`), Partial/Full PWJ, 대량 HASH HASH 분배, Bloom Filter 관련 문제를 1초 만에 풀어내기 위한 핵심 체크노트

---

# 🚨 [실전 직전 3분] 병렬 처리 & PWJ 5대 함정 탈출 체크리스트

```text
┌────────────────────────────────────────────────────────────────────────────────────────┐
│ ⚠️ [함정 1] 수십~수백만 건 대량 집합을 BROADCAST하여 DOP배로 뻥튀기 복제시키는 실수   │
│ 👉 탈출법: 대량 ↔ 대량은 무조건 [PQ_DISTRIBUTE(Inner, HASH, HASH)]로 균등 1/N 분할!   │
├────────────────────────────────────────────────────────────────────────────────────────┤
│ ⚠️ [함정 2] 양쪽 테이블이 동일 파티션인데 HASH HASH 재분배를 방치하는 실수             │
│ 👉 탈출법: [PQ_DISTRIBUTE(Inner, NONE, NONE)]로 통신량 0건의 [Full PWJ] 유도!         │
├────────────────────────────────────────────────────────────────────────────────────────┤
│ ⚠️ [함정 3] 한쪽만 파티션된 대량 테이블 조인 시 전체 재분배 부하를 유발하는 실수      │
│ 👉 탈출법: [PQ_DISTRIBUTE(Inner, PARTITION, NONE)]로 [Partial PWJ] 유도!              │
├────────────────────────────────────────────────────────────────────────────────────────┤
│ ⚠️ [함정 4] PQ_DISTRIBUTE 힌트 작성 시 LEADING 및 USE_HASH를 누락하는 실수             │
│ 👉 탈출법: PQ_DISTRIBUTE는 [/*+ LEADING(Outer Inner) USE_HASH(Inner) */]와 한 세트!  │
├────────────────────────────────────────────────────────────────────────────────────────┤
│ ⚠️ [함정 5] 병렬 DML 시 ALTER SESSION ENABLE PARALLEL DML을 누락하는 실수             │
│ 👉 탈출법: SELECT 병렬과 달리 DML 병렬은 세션 레벨의 PARALLEL DML 활성화가 필수 선행! │
└────────────────────────────────────────────────────────────────────────────────────────┘
```

---

# 🛡️ 1. `PQ_DISTRIBUTE` 3대 킬러 공식 요약표

| 조인 상황 및 물리적 구조 | 최적 분배 전략 | 표준 힌트 구문 | 물리적 효과 및 전송량 |
|---|---|---|---|
| **1. 대량 ↔ 소량 (수만 건 이하)** | **BROADCAST** | `/*+ LEADING(A B) USE_HASH(B) PQ_DISTRIBUTE(B, BROADCAST, NONE) */` | 소량(A)을 모든 슬레이브에 복제 전송, B는 로컬 스캔 |
| **2. 대량 ↔ 대량 (수백만 건 이상)** | **HASH HASH** | `/*+ LEADING(A B) USE_HASH(B) PQ_DISTRIBUTE(B, HASH, HASH) */` | 양쪽 모두 조인 키 기준 1/N 균등 분할 전송, 메모리 절감 |
| **3. 한쪽만 조인키 파티션 테이블** | **Partial PWJ** | `/*+ LEADING(A B) USE_HASH(B) PQ_DISTRIBUTE(B, PARTITION, NONE) */` | A를 B의 파티션 맵으로 1회 분배, B는 로컬 스캔 |
| **4. 양쪽 모두 조인키 동등 파티션** | **Full PWJ** | `/*+ LEADING(A B) USE_HASH(B) PQ_DISTRIBUTE(B, NONE, NONE) */` | **통신량 0건!** 파티션 쌍끼리 1:1 로컬 조인 |

---

# ⚡ 2. 1개 Slave Set vs 2개 Slave Set 판독법

* **1개 Slave Set (`Q1,00`만 존재, 총 DOP개 프로세스):**
  * `Full PWJ (NONE, NONE)`, `Partial PWJ (PARTITION, NONE)`, `BROADCAST (BROADCAST, NONE)`의 Inner 단계처럼 **생산자-소비자 분리 없이 한 세트가 순차 처리**할 때.
* **2개 Slave Set (`Q1,00`, `Q1,01` 동시 가동, 총 2 × DOP개 프로세스):**
  * `HASH HASH (HASH, HASH)` 분배처럼 양쪽 데이터를 서로 다른 슬레이브가 동시에 읽어 해시 교차 전송할 때.

---

# 💬 3. 실전 질의응답(Q&A) 마스터 카드

```text
┌────────────────────────────────────────────────────────────────────────────────────────┐
│ 💬 Q1. 왜 Full PWJ는 슬레이브 간 통신량이 0건인가요?                                  │
│ 👉 A1. 1월 파티션끼리, 2월 파티션끼리 이미 완벽히 물리적으로 분할되어 있으므로,         │
│        각 슬레이브가 자기가 맡은 파티션 쌍만 독립적으로 로컬 조인하기 때문입니다.     │
├────────────────────────────────────────────────────────────────────────────────────────┤
│ 💬 Q2. PQ_DISTRIBUTE 힌트의 인자 순서(Outer, Inner)를 쉽게 외우는 법은?               │
│ 👉 A2. PQ_DISTRIBUTE(Inner테이블명, Outer분배방식, Inner분배방식) 형태입니다.          │
│        항상 [바깥쪽(Outer)을 어떻게 보낼지]가 먼저 나오고, 그 다음 [안쪽(Inner)]입니다. │
└────────────────────────────────────────────────────────────────────────────────────────┘
```
